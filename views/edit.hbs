<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Beemp - Edit</title>
  </head>
  <body>
    <form action="/edit/{{message._id}}" method="post">
      <div class="form-group">
        <label>Name</label><input type="text" name="name" value="{{message.name}}">
      </div>
      <div class="form-group">
        <label>Message</label><input type="text" name="message" value="{{message.message}}">
      </div>
      <button type="submit">Update</button>
  </body>
</html>
